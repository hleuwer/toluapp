cmake_minimum_required(VERSION 2.8)
project(tolua++)
set(luavers "52")
set(progname "tolua++${luavers}")
set(libname "libtolua++${luavers}")
set(bootstrap "tolua++_bootstrap${luavers}")
enable_testing()
add_subdirectory(src)
add_subdirectory(include)
